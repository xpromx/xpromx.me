import React, { FC } from "react";
import { Layout, Title } from "@components";

const Page: FC = () => {
  return (
    <Layout title="Uses">
      <Layout.Main>
        <Title>Uses</Title>
        ...
      </Layout.Main>
    </Layout>
  );
};

export default Page;
