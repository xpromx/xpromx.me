import React, { FC } from "react";
import { Layout, Title } from "@components";

const Page: FC = () => {
  return (
    <Layout title="Contact">
      <Layout.Main>
        <Title>Contact</Title>
        ...
      </Layout.Main>
    </Layout>
  );
};

export default Page;
